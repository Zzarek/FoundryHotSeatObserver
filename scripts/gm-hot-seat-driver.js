Hooks.on("controlToken", (token, isControlled) => {
      if (game.settings.get('foundry-hot-seat-observer', 'hotSeatObserver') && game.settings.get('foundry-hot-seat-observer', 'toggleGMSelect')) {

      }

	let x = 2;
});

